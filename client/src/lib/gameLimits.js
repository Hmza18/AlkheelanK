/** Soft cap shown to hosts — game still accepts joins up to MAX_PLAYERS on the server. */
export const RECOMMENDED_MAX_PLAYERS = 55;

/** Hard server cap (see server/src/gameManager.js). */
export const MAX_PLAYERS = 100;
