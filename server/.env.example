# Port the server listens on (Render sets this automatically)
PORT=3001

# Allowed client origin(s) for CORS. Comma-separate multiple origins.
# Use * for local dev. In production set this to your Vercel URL, e.g.
# CORS_ORIGIN=https://alkheelank.vercel.app
CORS_ORIGIN=*
